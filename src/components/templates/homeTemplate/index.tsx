import React, { FC } from 'react'

const HomeTemplate: FC = () => {
  return <div>Hello, this is a template form ANTD, tailwind, eslint, stroybook, using typescript and react.</div>
}

export default HomeTemplate
